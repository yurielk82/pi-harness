---
description: Load foundational context for the pi-harness codebase
---

# Purpose

Orient yourself in `pi-harness` — a standalone Pi and Codex-friendly multi-agent harness focused on team orchestration, safety, workflow artifacts, and review discipline.

## Workflow

1. Read `README.md`, `justfile`, and `package.json`
2. Read `extensions/*`
3. Read `.pi/agents/*`
4. Read `.pi/workflows/*` and `.pi/memory/*`
5. Read `.pi/settings.json` and `bin/*`
6. Summarize the project: purpose, stack, commands, workflow layers, and main extension entry points
